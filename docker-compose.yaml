services:
  redis:
    container_name: "redis"
    image: redis:alpine
    command: redis-server --requirepass mypassword
    ports: 
      - "6379:6379"

  go:
    container_name: "backend"
    build:
      context: ./backend
    ports:
      - "8080:8080"